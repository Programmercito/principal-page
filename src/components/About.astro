---
import { Icon } from 'astro-icon/components';
---

<section id="about" class="py-28 relative overflow-hidden">
  <div class="about-bg"></div>

  <div class="container mx-auto px-6 relative z-10">
    <div class="about-grid">
      <!-- Left: Info -->
      <div class="fade-in-left">
        <span class="section-label-about">Sobre mí</span>
        <h2 class="about-title">
          Transformo ideas en
          <span class="gradient-text"> experiencias digitales</span>
        </h2>
        <p class="about-text">
          Soy un apasionado desarrollador web especializado en la creación de
          soluciones informáticas a medida. Diseño e implemento sistemas empresariales,
          APIs robustas y aplicaciones web de alto rendimiento para impulsar el
          crecimiento de empresas y negocios.
        </p>
        <p class="about-text">
          Con dominio en Java, Laravel y Angular, construyo arquitecturas sólidas
          tanto en el backend como en el frontend. Me especializo en integración
          de APIs, sistemas ERP y consultoría tecnológica estratégica.
        </p>

        <!-- Skills chips -->
        <div class="skills-wrap">
          {["Java", "Laravel", "Angular", "MySQL", "REST APIs", "Docker", "Linux", "Astro"].map(skill => (
            <span class="skill-chip">{skill}</span>
          ))}
        </div>

        <!-- Social links -->
        <div class="social-links-row">
          <a href="https://github.com/programmercito" target="_blank" rel="noopener noreferrer" class="social-btn" aria-label="GitHub">
            <Icon name="mdi:github" class="w-5 h-5" />
            <span>GitHub</span>
          </a>
          <a href="https://www.linkedin.com/in/joaquin-h-m/" target="_blank" rel="noopener noreferrer" class="social-btn" aria-label="LinkedIn">
            <Icon name="mdi:linkedin" class="w-5 h-5" />
            <span>LinkedIn</span>
          </a>
          <a href="https://x.com/programmercito" target="_blank" rel="noopener noreferrer" class="social-btn" aria-label="X / Twitter">
            <Icon name="simple-icons:x" class="w-4 h-4" />
            <span>Twitter</span>
          </a>
          <a href="https://portafolio.devcito.org/" target="_blank" rel="noopener noreferrer" class="social-btn" aria-label="Portfolio">
            <Icon name="mdi:briefcase" class="w-5 h-5" />
            <span>Portafolio</span>
          </a>
        </div>
      </div>

      <!-- Right: Stats/Cards -->
      <div class="about-cards fade-in-element">
        <div class="stat-card">
          <div class="stat-card-icon">
            <Icon name="mdi:code-tags" class="w-7 h-7 text-cyan-400" />
          </div>
          <div>
            <p class="stat-card-number">10+</p>
            <p class="stat-card-label">Proyectos completados</p>
          </div>
        </div>
        <div class="stat-card">
          <div class="stat-card-icon">
            <Icon name="mdi:account-check" class="w-7 h-7 text-purple-400" />
          </div>
          <div>
            <p class="stat-card-number">100%</p>
            <p class="stat-card-label">Clientes satisfechos</p>
          </div>
        </div>
        <div class="stat-card">
          <div class="stat-card-icon">
            <Icon name="mdi:lightning-bolt" class="w-7 h-7 text-amber-400" />
          </div>
          <div>
            <p class="stat-card-number">5+</p>
            <p class="stat-card-label">Años de experiencia</p>
          </div>
        </div>
        <div class="stat-card">
          <div class="stat-card-icon">
            <Icon name="mdi:headset" class="w-7 h-7 text-green-400" />
          </div>
          <div>
            <p class="stat-card-number">24/7</p>
            <p class="stat-card-label">Soporte técnico</p>
          </div>
        </div>

        <!-- Decorative element -->
        <div class="about-deco">
          <div class="deco-circle"></div>
          <div class="deco-circle deco-2"></div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about-bg {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse 50% 70% at 100% 50%, rgba(139, 92, 246, 0.06), transparent),
      rgba(13, 18, 39, 0.2);
    pointer-events: none;
  }

  .section-label-about {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: #22d3ee;
    background: rgba(34, 211, 238, 0.1);
    border: 1px solid rgba(34, 211, 238, 0.2);
    border-radius: 20px;
    padding: 4px 16px;
    margin-bottom: 1.25rem;
    display: inline-block;
  }

  .about-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
  }

  @media (max-width: 900px) {
    .about-grid {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
  }

  @media (max-width: 640px) {
    .about-grid {
      gap: 2rem;
    }
  }

  .about-title {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 2.5rem;
    font-weight: 700;
    color: white;
    line-height: 1.2;
    letter-spacing: -0.02em;
    margin-bottom: 1.5rem;
  }

  @media (max-width: 640px) {
    .about-title {
      font-size: 1.85rem;
    }
  }

  .gradient-text {
    background: linear-gradient(135deg, #22d3ee, #8b5cf6);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .about-text {
    color: #94a3b8;
    line-height: 1.7;
    font-size: 0.95rem;
    margin-bottom: 1rem;
  }

  .skills-wrap {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin: 1.5rem 0;
  }

  .skill-chip {
    font-size: 0.75rem;
    font-weight: 600;
    padding: 5px 14px;
    border-radius: 20px;
    background: rgba(34, 211, 238, 0.08);
    border: 1px solid rgba(34, 211, 238, 0.2);
    color: #67e8f9;
    letter-spacing: 0.03em;
    transition: all 0.2s ease;
  }

  .skill-chip:hover {
    background: rgba(34, 211, 238, 0.15);
    border-color: rgba(34, 211, 238, 0.5);
    transform: translateY(-2px);
  }

  .social-links-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 1.5rem;
  }

  .social-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 8px 16px;
    border-radius: 10px;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    color: #94a3b8;
    font-size: 0.85rem;
    font-weight: 500;
    text-decoration: none;
    transition: all 0.3s ease;
  }

  .social-btn:hover {
    background: rgba(34, 211, 238, 0.1);
    border-color: rgba(34, 211, 238, 0.3);
    color: #22d3ee;
    transform: translateY(-2px);
  }

  /* Right side cards */
  .about-cards {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    position: relative;
  }

  @media (max-width: 900px) {
    .about-cards {
      grid-template-columns: 1fr 1fr;
      gap: 0.75rem;
    }
  }

  @media (max-width: 640px) {
    .about-cards {
      grid-template-columns: 1fr;
    }
  }

  .stat-card {
    background: rgba(13, 18, 39, 0.7);
    border: 1px solid rgba(255, 255, 255, 0.07);
    border-radius: 16px;
    padding: 1.25rem;
    display: flex;
    align-items: center;
    gap: 0.75rem;
    backdrop-filter: blur(10px);
    transition: all 0.3s ease;
    min-width: 0;
  }

  .stat-card:hover {
    border-color: rgba(34, 211, 238, 0.2);
    background: rgba(34, 211, 238, 0.04);
    transform: translateY(-3px);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.3);
  }

  .stat-card-icon {
    width: 44px;
    height: 44px;
    border-radius: 12px;
    background: rgba(255, 255, 255, 0.04);
    border: 1px solid rgba(255, 255, 255, 0.08);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .stat-card-number {
    font-family: 'Space Grotesk', sans-serif;
    font-size: 1.35rem;
    font-weight: 700;
    color: white;
    line-height: 1;
    margin-bottom: 0.2rem;
    white-space: nowrap;
  }

  .stat-card-label {
    font-size: 0.7rem;
    color: #64748b;
    line-height: 1.3;
    word-break: break-word;
  }

  .about-deco {
    position: absolute;
    inset: 0;
    pointer-events: none;
    overflow: hidden;
    border-radius: 20px;
    z-index: -1;
  }

  .deco-circle {
    position: absolute;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(34, 211, 238, 0.1), transparent 60%);
    width: 300px;
    height: 300px;
    top: -100px;
    right: -100px;
    filter: blur(40px);
  }

  .deco-2 {
    background: radial-gradient(circle, rgba(139, 92, 246, 0.08), transparent 60%);
    bottom: -80px;
    left: -80px;
    top: auto;
    right: auto;
  }
</style>