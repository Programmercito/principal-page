---
import { Icon } from 'astro-icon/components';
---

<section id="about" class="py-28 relative overflow-hidden">
  <div class="about-bg"></div>
  <!-- Floating orbs -->
  <div class="orb orb-1"></div>
  <div class="orb orb-2"></div>

  <div class="container mx-auto px-6 relative z-10">
    <div class="about-grid">
      <!-- Left: Info -->
      <div class="fade-slide-left">
        <span class="section-label-about">Sobre mí</span>
        <h2 class="about-title">
          Transformo ideas en
          <span class="gradient-text"> experiencias digitales</span>
        </h2>
        <p class="about-text">
          Soy un apasionado desarrollador web especializado en la creación de
          soluciones informáticas a medida. Diseño e implemento sistemas empresariales,
          APIs robustas y aplicaciones web de alto rendimiento.
        </p>
        <p class="about-text">
          Con dominio en Java, Laravel y Angular, construyo arquitecturas sólidas
          tanto en el backend como en el frontend. Me especializo en integración
          de APIs, sistemas ERP y consultoría tecnológica estratégica.
        </p>

        <!-- Skills con barras -->
        <div class="skills-section">
          <div class="skill-bar-item">
            <div class="skill-bar-header">
              <span class="skill-bar-name">Backend (Laravel / Java)</span>
              <span class="skill-bar-pct">90%</span>
            </div>
            <div class="skill-bar-track">
              <div class="skill-bar-fill" style="--pct:90%; --color:#22d3ee"></div>
            </div>
          </div>
          <div class="skill-bar-item">
            <div class="skill-bar-header">
              <span class="skill-bar-name">Frontend (Angular)</span>
              <span class="skill-bar-pct">85%</span>
            </div>
            <div class="skill-bar-track">
              <div class="skill-bar-fill" style="--pct:85%; --color:#3b82f6"></div>
            </div>
          </div>
          <div class="skill-bar-item">
            <div class="skill-bar-header">
              <span class="skill-bar-name">Sistemas ERP / APIs</span>
              <span class="skill-bar-pct">88%</span>
            </div>
            <div class="skill-bar-track">
              <div class="skill-bar-fill" style="--pct:88%; --color:#8b5cf6"></div>
            </div>
          </div>
          <div class="skill-bar-item">
            <div class="skill-bar-header">
              <span class="skill-bar-name">DevOps (Docker / Linux)</span>
              <span class="skill-bar-pct">75%</span>
            </div>
            <div class="skill-bar-track">
              <div class="skill-bar-fill" style="--pct:75%; --color:#f59e0b"></div>
            </div>
          </div>
        </div>

        <!-- Tech chips -->
        <div class="skills-wrap">
          {["Java", "Laravel", "Go", "Angular", "MySQL", "REST APIs", "Docker", "Linux", "Astro"].map(skill => (
            <span class="skill-chip">{skill}</span>
          ))}
        </div>

        <!-- Social links -->
        <div class="social-links-row">
          <a href="https://github.com/programmercito" target="_blank" rel="noopener noreferrer" class="social-btn" aria-label="GitHub">
            <Icon name="mdi:github" class="w-5 h-5" />
            <span>GitHub</span>
          </a>
          <a href="https://www.linkedin.com/in/joaquin-h-m/" target="_blank" rel="noopener noreferrer" class="social-btn" aria-label="LinkedIn">
            <Icon name="mdi:linkedin" class="w-5 h-5" />
            <span>LinkedIn</span>
          </a>
          <a href="https://x.com/programmercito" target="_blank" rel="noopener noreferrer" class="social-btn" aria-label="X / Twitter">
            <Icon name="simple-icons:x" class="w-4 h-4" />
            <span>Twitter</span>
          </a>
          <a href="https://portafolio.devcito.org/" target="_blank" rel="noopener noreferrer" class="social-btn" aria-label="Portfolio">
            <Icon name="mdi:briefcase" class="w-5 h-5" />
            <span>Portafolio</span>
          </a>
        </div>
      </div>

      <!-- Right: Stats/Cards -->
      <div class="about-cards fade-slide-right">
        <div class="stat-card stat-card-cyan">
          <div class="stat-card-icon">
            <Icon name="mdi:code-tags" class="w-7 h-7" />
          </div>
          <div>
            <p class="stat-card-number">10+</p>
            <p class="stat-card-label">Proyectos completados</p>
          </div>
          <div class="stat-card-glow"></div>
        </div>
        <div class="stat-card stat-card-purple">
          <div class="stat-card-icon">
            <Icon name="mdi:account-check" class="w-7 h-7" />
          </div>
          <div>
            <p class="stat-card-number">100%</p>
            <p class="stat-card-label">Clientes satisfechos</p>
          </div>
          <div class="stat-card-glow"></div>
        </div>
        <div class="stat-card stat-card-amber">
          <div class="stat-card-icon">
            <Icon name="mdi:lightning-bolt" class="w-7 h-7" />
          </div>
          <div>
            <p class="stat-card-number">5+</p>
            <p class="stat-card-label">Años de experiencia</p>
          </div>
          <div class="stat-card-glow"></div>
        </div>
        <div class="stat-card stat-card-green">
          <div class="stat-card-icon">
            <Icon name="mdi:headset" class="w-7 h-7" />
          </div>
          <div>
            <p class="stat-card-number">24/7</p>
            <p class="stat-card-label">Soporte técnico</p>
          </div>
          <div class="stat-card-glow"></div>
        </div>

        <!-- Decorative element -->
        <div class="about-deco">
          <div class="deco-circle"></div>
          <div class="deco-circle deco-2"></div>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .about-bg {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse 50% 70% at 100% 50%, rgba(139, 92, 246, 0.07), transparent),
      radial-gradient(ellipse 40% 60% at 0% 50%, rgba(34, 211, 238, 0.04), transparent);
    pointer-events: none;
  }

  .orb {
    position: absolute;
    border-radius: 50%;
    filter: blur(100px);
    pointer-events: none;
    animation: orb-float 20s ease-in-out infinite alternate;
  }
  .orb-1 {
    width: 350px; height: 350px;
    background: radial-gradient(circle, rgba(34,211,238,0.07), transparent 70%);
    top: -100px; right: 10%;
  }
  .orb-2 {
    width: 250px; height: 250px;
    background: radial-gradient(circle, rgba(139,92,246,0.07), transparent 70%);
    bottom: -80px; left: 5%;
    animation-direction: alternate-reverse;
  }
  @keyframes orb-float {
    from { transform: translate(0,0); }
    to { transform: translate(30px, -40px); }
  }

  .section-label-about {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: #22d3ee;
    background: rgba(34, 211, 238, 0.1);
    border: 1px solid rgba(34, 211, 238, 0.25);
    border-radius: 20px;
    padding: 5px 18px;
    margin-bottom: 1.25rem;
    box-shadow: 0 0 15px rgba(34,211,238,0.1);
  }

  .about-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 5rem;
    align-items: center;
    max-width: 1200px;
    margin: 0 auto;
  }

  @media (max-width: 900px) {
    .about-grid { grid-template-columns: 1fr; gap: 3rem; }
  }

  .about-title {
    font-family: 'Outfit', sans-serif;
    font-size: clamp(1.8rem, 3.5vw, 2.6rem);
    font-weight: 700;
    color: white;
    line-height: 1.2;
    letter-spacing: -0.02em;
    margin-bottom: 1.5rem;
  }

  .gradient-text {
    background: linear-gradient(135deg, #22d3ee, #8b5cf6);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
  }

  .about-text {
    color: #94a3b8;
    line-height: 1.75;
    font-size: 0.95rem;
    margin-bottom: 1rem;
  }

  /* Skill bars */
  .skills-section {
    display: flex;
    flex-direction: column;
    gap: 1rem;
    margin: 1.75rem 0;
  }

  .skill-bar-item { display: flex; flex-direction: column; gap: 6px; }

  .skill-bar-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
  }

  .skill-bar-name {
    font-size: 0.8rem;
    font-weight: 600;
    color: #94a3b8;
    letter-spacing: 0.02em;
  }

  .skill-bar-pct {
    font-size: 0.75rem;
    font-weight: 700;
    color: #64748b;
    font-family: 'Outfit', sans-serif;
  }

  .skill-bar-track {
    height: 6px;
    background: rgba(255,255,255,0.06);
    border-radius: 10px;
    overflow: hidden;
  }

  .skill-bar-fill {
    height: 100%;
    border-radius: 10px;
    background: linear-gradient(90deg, var(--color), color-mix(in srgb, var(--color) 60%, white 40%));
    width: 0;
    animation: bar-fill 1.2s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    animation-delay: 0.3s;
    box-shadow: 0 0 8px color-mix(in srgb, var(--color) 70%, transparent 30%);
  }

  @keyframes bar-fill { to { width: var(--pct); } }

  /* Tech chips */
  .skills-wrap {
    display: flex;
    flex-wrap: wrap;
    gap: 0.5rem;
    margin-bottom: 1.5rem;
  }

  .skill-chip {
    font-size: 0.72rem;
    font-weight: 600;
    padding: 5px 14px;
    border-radius: 20px;
    background: rgba(34, 211, 238, 0.07);
    border: 1px solid rgba(34, 211, 238, 0.18);
    color: #67e8f9;
    letter-spacing: 0.03em;
    transition: all 0.25s ease;
    cursor: default;
  }
  .skill-chip:hover {
    background: rgba(34, 211, 238, 0.16);
    border-color: rgba(34, 211, 238, 0.5);
    transform: translateY(-3px);
    box-shadow: 0 6px 15px rgba(34,211,238,0.15);
  }

  /* Social */
  .social-links-row {
    display: flex;
    flex-wrap: wrap;
    gap: 0.75rem;
    margin-top: 0;
  }

  .social-btn {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    padding: 8px 16px;
    border-radius: 12px;
    background: rgba(255,255,255,0.04);
    border: 1px solid rgba(255,255,255,0.08);
    color: #94a3b8;
    font-size: 0.85rem;
    font-weight: 500;
    text-decoration: none;
    transition: all 0.3s ease;
  }
  .social-btn:hover {
    background: rgba(34,211,238,0.1);
    border-color: rgba(34,211,238,0.35);
    color: #22d3ee;
    transform: translateY(-3px);
    box-shadow: 0 8px 20px rgba(34,211,238,0.12);
  }

  /* Stat Cards */
  .about-cards {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 1rem;
    position: relative;
  }

  @media (max-width: 640px) {
    .about-cards { grid-template-columns: 1fr; }
  }

  .stat-card {
    background: rgba(8, 14, 32, 0.7);
    border: 1px solid rgba(255,255,255,0.07);
    border-radius: 18px;
    padding: 1.5rem;
    display: flex;
    align-items: center;
    gap: 1rem;
    backdrop-filter: blur(16px);
    transition: all 0.35s cubic-bezier(0.34, 1.56, 0.64, 1);
    position: relative;
    overflow: hidden;
  }

  .stat-card:hover {
    transform: translateY(-6px) scale(1.02);
    box-shadow: 0 20px 50px rgba(0,0,0,0.35);
  }

  .stat-card-glow {
    position: absolute;
    inset: 0;
    opacity: 0;
    transition: opacity 0.4s ease;
    pointer-events: none;
    border-radius: 18px;
  }
  .stat-card:hover .stat-card-glow { opacity: 1; }

  .stat-card-cyan {
    border-color: rgba(34,211,238,0.15);
  }
  .stat-card-cyan:hover { border-color: rgba(34,211,238,0.4); box-shadow: 0 20px 50px rgba(0,0,0,0.35), 0 0 30px rgba(34,211,238,0.1); }
  .stat-card-cyan .stat-card-icon { color: #22d3ee; background: rgba(34,211,238,0.1); border-color: rgba(34,211,238,0.2); }
  .stat-card-cyan .stat-card-glow { background: radial-gradient(ellipse at top left, rgba(34,211,238,0.07), transparent 70%); }

  .stat-card-purple { border-color: rgba(139,92,246,0.15); }
  .stat-card-purple:hover { border-color: rgba(139,92,246,0.4); box-shadow: 0 20px 50px rgba(0,0,0,0.35), 0 0 30px rgba(139,92,246,0.1); }
  .stat-card-purple .stat-card-icon { color: #a78bfa; background: rgba(139,92,246,0.1); border-color: rgba(139,92,246,0.2); }
  .stat-card-purple .stat-card-glow { background: radial-gradient(ellipse at top left, rgba(139,92,246,0.07), transparent 70%); }

  .stat-card-amber { border-color: rgba(245,158,11,0.15); }
  .stat-card-amber:hover { border-color: rgba(245,158,11,0.4); box-shadow: 0 20px 50px rgba(0,0,0,0.35), 0 0 30px rgba(245,158,11,0.08); }
  .stat-card-amber .stat-card-icon { color: #fbbf24; background: rgba(245,158,11,0.1); border-color: rgba(245,158,11,0.2); }
  .stat-card-amber .stat-card-glow { background: radial-gradient(ellipse at top left, rgba(245,158,11,0.06), transparent 70%); }

  .stat-card-green { border-color: rgba(34,197,94,0.15); }
  .stat-card-green:hover { border-color: rgba(34,197,94,0.4); box-shadow: 0 20px 50px rgba(0,0,0,0.35), 0 0 30px rgba(34,197,94,0.1); }
  .stat-card-green .stat-card-icon { color: #4ade80; background: rgba(34,197,94,0.1); border-color: rgba(34,197,94,0.2); }
  .stat-card-green .stat-card-glow { background: radial-gradient(ellipse at top left, rgba(34,197,94,0.07), transparent 70%); }

  .stat-card-icon {
    width: 52px; height: 52px;
    border-radius: 14px;
    border: 1px solid;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    transition: transform 0.35s cubic-bezier(0.34, 1.56, 0.64, 1);
  }
  .stat-card:hover .stat-card-icon { transform: scale(1.15) rotate(-5deg); }

  .stat-card-number {
    font-family: 'Outfit', sans-serif;
    font-size: 1.5rem;
    font-weight: 800;
    color: white;
    line-height: 1;
    margin-bottom: 0.2rem;
  }

  .stat-card-label {
    font-size: 0.7rem;
    color: #64748b;
    line-height: 1.3;
  }

  .about-deco {
    position: absolute; inset: 0;
    pointer-events: none; overflow: hidden;
    border-radius: 20px; z-index: -1;
  }

  .deco-circle {
    position: absolute;
    border-radius: 50%;
    background: radial-gradient(circle, rgba(34,211,238,0.08), transparent 60%);
    width: 300px; height: 300px;
    top: -120px; right: -120px;
    filter: blur(50px);
  }
  .deco-2 {
    background: radial-gradient(circle, rgba(139,92,246,0.07), transparent 60%);
    bottom: -80px; left: -80px;
    top: auto; right: auto;
  }

  /* Animations */
  .fade-slide-left {
    opacity: 0; transform: translateX(-30px);
    animation: slide-from-left 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    animation-delay: 0.2s;
  }
  .fade-slide-right {
    opacity: 0; transform: translateX(30px);
    animation: slide-from-right 0.8s cubic-bezier(0.16, 1, 0.3, 1) forwards;
    animation-delay: 0.35s;
  }
  @keyframes slide-from-left { to { opacity: 1; transform: translateX(0); } }
  @keyframes slide-from-right { to { opacity: 1; transform: translateX(0); } }
</style>