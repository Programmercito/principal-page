---
import { Icon } from 'astro-icon/components';
import Button from './Button.astro';

interface Props {
  icon: string;
  title: string;
  description: string;
}

const { icon, title, description } = Astro.props;
---

<div class="bg-gray-800 p-8 rounded-lg shadow-lg text-center h-full flex flex-col">
  <Icon name={icon} class="w-16 h-16 mx-auto mb-6 text-cyan-400" />
  <h3 class="text-2xl font-bold text-white mb-4">{title}</h3>
  <p class="text-gray-400 mb-6 flex-grow">{description}</p>
  <Button href="#contact" label="Ver más" className="mt-auto" />
</div>
