---
import ServiceCard from './ServiceCard.astro';
---

<section id="services" class="py-28 relative">
  <!-- Background -->
  <div class="services-bg"></div>

  <!-- Header — dentro del container con padding normal -->
  <div class="container mx-auto px-6 relative z-10">
    <div class="text-center mb-16 fade-in-element">
      <span class="section-label">¿Qué hacemos?</span>
      <h2 class="section-title">Nuestros Servicios</h2>
      <p class="section-subtitle">
        Soluciones digitales integrales para llevar tu negocio al siguiente nivel.
      </p>
    </div>

    <!-- Scroll hint (mobile only) -->
    <div class="scroll-hint">
      <span>deslizá para ver más</span>
      <svg xmlns="http://www.w3.org/2000/svg" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <path d="M5 12h14m-7-7 7 7-7 7"/>
      </svg>
    </div>
  </div>

  <!-- Track — hijo directo de la sección, sin el px-6 del container -->
  <div class="services-track-wrap relative z-10">
    <div class="services-track">
      <div class="service-slide"><ServiceCard icon="mdi:web" title="Desarrollo Web" description="Desarrollo sitios web, applications y sistemas a medida. Desde landing pages hasta plataformas empresariales complejas, enfocados en rendimiento, escalabilidad y experiencia de usuario." gradient="from-emerald-500 to-teal-500" accentColor="#10b981" /></div>
      <div class="service-slide"><ServiceCard icon="mdi:cogs" title="Sistemas ERP" description="Implemento y personalizo sistemas ERP para integrar y automatizar los procesos clave de tu empresa: inventario, facturación, RRHH y más, en una sola plataforma centralizada." gradient="from-cyan-500 to-blue-500" accentColor="#06b6d4" /></div>
      <div class="service-slide"><ServiceCard icon="mdi:robot-outline" title="Automatización de Procesos" description="Identifico tareas repetitivas y las automatizo con scripts, bots y flujos inteligentes. Reducí costos operativos, errores humanos y ganás tiempo para lo que realmente importa." gradient="from-purple-500 to-violet-500" accentColor="#8b5cf6" /></div>
      <div class="service-slide"><ServiceCard icon="mdi:monitor-dashboard" title="Consultoría Informática" description="Asesoramiento tecnológico integral para empresas y negocios. Te ayudo a elegir herramientas, arquitecturas y soluciones digitales alineadas a tus objetivos y presupuesto." gradient="from-rose-500 to-pink-500" accentColor="#f43f5e" /></div>
      <div class="service-slide"><ServiceCard icon="mdi:bullhorn" title="Marketing Digital" description="Potencio tu presencia en línea a través de gestión estratégica de redes sociales, SEO y campañas publicitarias. Aumentá tu visibilidad y atraé más clientes." gradient="from-amber-500 to-orange-500" accentColor="#f59e0b" /></div>
    </div>
  </div>

  <!-- Dots — de vuelta en el container -->
  <div class="container mx-auto px-6 relative z-10">
    <div class="mobile-dots" id="svc-dots">
      <span class="mdot active"></span>
      <span class="mdot"></span>
      <span class="mdot"></span>
      <span class="mdot"></span>
      <span class="mdot"></span>
    </div>
  </div>
</section>

<script>
  // Update active dot on mobile scroll
  const track = document.querySelector('.services-track') as HTMLElement;
  const dots = document.querySelectorAll('.mdot');
  if (track && dots.length) {
    track.addEventListener('scroll', () => {
      const slideWidth = track.scrollWidth / dots.length;
      const active = Math.round(track.scrollLeft / slideWidth);
      dots.forEach((d, i) => d.classList.toggle('active', i === active));
    }, { passive: true });
  }
</script>

<style>
  .services-bg {
    position: absolute;
    inset: 0;
    background:
      radial-gradient(ellipse 60% 80% at 0% 50%, rgba(16, 185, 129, 0.04), transparent),
      radial-gradient(ellipse 60% 80% at 100% 50%, rgba(139, 92, 246, 0.04), transparent),
      rgba(3, 7, 18, 0.3);
    pointer-events: none;
  }

  .section-label {
    display: inline-block;
    font-size: 0.75rem;
    font-weight: 600;
    letter-spacing: 0.15em;
    text-transform: uppercase;
    color: #10b981;
    background: rgba(16, 185, 129, 0.1);
    border: 1px solid rgba(16, 185, 129, 0.2);
    border-radius: 20px;
    padding: 4px 16px;
    margin-bottom: 1rem;
  }

  /* Scroll hint — mobile only */
  .scroll-hint {
    display: none;
    justify-content: center;
    align-items: center;
    gap: 6px;
    font-size: 0.72rem;
    font-weight: 500;
    color: #475569;
    letter-spacing: 0.04em;
    margin-bottom: 1rem;
  }

  /* Track wrapper */
  .services-track-wrap {
    max-width: 1400px;
    margin: 0 auto;
  }

  /* Desktop: single row, all 5 equal width */
  .services-track {
    display: grid;
    grid-template-columns: repeat(5, 1fr);
    gap: 1rem;
  }

  .service-slide {
    min-width: 0; /* prevent overflow */
    display: flex;
  }

  /* Mobile dots — hidden on desktop */
  .mobile-dots {
    display: none;
    justify-content: center;
    gap: 6px;
    margin-top: 1.25rem;
  }

  .mdot {
    width: 6px; height: 6px;
    border-radius: 50%;
    background: rgba(255,255,255,0.15);
    transition: all 0.3s ease;
    cursor: pointer;
  }
  .mdot.active {
    background: #10b981;
    width: 20px;
    border-radius: 4px;
    box-shadow: 0 0 8px rgba(16,185,129,0.5);
  }

  /* ── MOBILE ── */
  @media (max-width: 900px) {
    .scroll-hint {
      display: flex;
    }

    /* El track ya es hijo directo de la sección — ancho 100% sin márgenes negativos */
    .services-track-wrap {
      width: 100%;
      overflow: hidden;
    }

    .services-track {
      display: flex;
      overflow-x: auto;
      scroll-snap-type: x mandatory;
      scroll-behavior: smooth;
      gap: 0.75rem;
      padding: 0.5rem 1rem 1rem;
      scrollbar-width: none;
      -ms-overflow-style: none;
    }
    .services-track::-webkit-scrollbar { display: none; }

    .service-slide {
      /* ancho = pantalla completa menos el padding del track en ambos lados */
      flex: 0 0 calc(100% - 2.75rem);
      scroll-snap-align: start;
    }

    .mobile-dots {
      display: flex;
    }
  }

  @media (max-width: 480px) {
    .service-slide {
      flex: 0 0 calc(100% - 2.25rem);
    }
  }
</style>
